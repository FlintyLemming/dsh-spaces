# 团队空间实施计划（计划 05 / 共 07）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现团队空间：创建（任何用户，创建者为 owner）、按邮箱添加已注册成员、移除成员（级联清理其实例与私有卷）、删除空间（实例→私有卷→共享卷→行，重试 + 失败标 error），以及对应前端界面。

**Architecture:** 在计划 03 的 `src/spaces/` 模块上扩展：新增 `src/spaces/team.js`（业务规则与级联）与 `src/spaces/team-routes.js`（REST 路由，挂同一 `/api/spaces` 前缀），docker 侧新增 `removeVolume`。删除/移除级联严格按 spec §5 顺序执行，docker 步骤失败重试 3 次后标记 error 并写审计。前端新增创建对话框与成员管理面板。

**Tech Stack:** Fastify 5 + zod（路由）、better-sqlite3（store）、dockerode（卷操作）、Vitest（`:memory:` 库 + vi.mock 编排层）、React 18 + TS（前端）。

**Spec:** `docs/superpowers/specs/2026-09-04-dsh-spaces-design.md`（§3 规则、§5 空间生命周期、§8 UI、§9 错误处理）

**前置计划：** 01 骨架（schema/配置/错误格式/命名规则）→ 02 认证（`requireUser`、`getUserByEmail`、前端 `api.ts`）→ 03 空间与编排（`src/spaces/routes.js`、store spaces/volumes/instances、`ensureVolume`/`prepareSharedVolume`/`stopInstance`/`removeContainer`、`SpaceList.tsx`/`SpaceDetail.tsx`）。本计划与 04 互不依赖；06 的管理端复用本计划的 `deleteSpaceCascade`。

## Global Constraints

- 沿用计划 01 的全部约定：ESM JS、prepared statements、zod 校验、错误信封 `{ error: { code, message } }`、Conventional Commits、命名规则（`dshvol-<slug>-shared` / `dshvol-<slug>-<handle>` / `dsh-<slug>-<handle>`）。
- 个人空间（`kind='personal'`）拒绝一切成员操作与删除操作，统一 400 `PERSONAL_SPACE`（spec §5：个人空间不可删除）。
- 邀请仅限已注册用户（`users` 表存在该 email），否则 404 `USER_NOT_REGISTERED`；v1 不做邮件邀请（spec §5）。
- 删除空间/移除成员的 docker 步骤顺序不可交换：实例容器 → 私有卷 → 共享卷 → DB 行（spec §5，禁止残留孤儿资源）。
- 每个 docker 步骤最多 3 次尝试、间隔 500ms；最终失败标记相关实例 `error`、写审计 `space.delete_failed`、返回 502 `SPACE_DELETE_FAILED`。残留孤儿卷/容器的发现入口在计划 06 用量页（不在本计划实现）。
- UI 遵循 spec §8：单色、语义化表格、错误消息直接展示 API 信封的 `message`。

---

### Task 1: store 辅助函数核对与补齐

**Files:**
- Modify: `src/store/spaces.js`（补 `removeSpaceMember`、`deleteSpace`）
- Modify: `src/store/volumes.js`（补 `deleteVolume`）
- Modify: `src/store/instances.js`（补 `deleteInstance`）
- Test: `test/store-team-helpers.test.js`

**Interfaces:**
- Consumes: 计划 03 的 store 模块。
- Produces（本计划后续任务与计划 06 依赖）:
  - `removeSpaceMember(spaceId, userId)` — 删成员行
  - `deleteSpace(id)` — 单事务删除该空间的 instances、volumes、space_members、spaces 行
  - `deleteVolume(id)` — 删卷行
  - `deleteInstance(id)` — 删实例行
- 若计划 03 已实现同名同签名函数，保留计划 03 版本，本任务只保留对应测试（测试即契约核对）。

- [ ] **Step 1: 写失败的测试**

```javascript
// test/store-team-helpers.test.js
import { test, beforeEach } from 'vitest'
import assert from 'node:assert/strict'
import { initDb } from '../src/store/db.js'
import { createUser } from '../src/store/users.js'
import {
  createSpace, addSpaceMember, getSpaceMember, listSpaceMembers,
  removeSpaceMember, deleteSpace, getSpaceBySlug,
} from '../src/store/spaces.js'
import { createVolume, listVolumesForSpace, deleteVolume } from '../src/store/volumes.js'
import { upsertInstance, listInstancesForSpace, deleteInstance } from '../src/store/instances.js'

let ownerId, memberId
beforeEach(() => {
  initDb(':memory:')
  ownerId = createUser({ email: 'o@x.com', handle: 'owner', displayName: 'O' })
  memberId = createUser({ email: 'm@x.com', handle: 'member', displayName: 'M' })
})

function seedTeamSpace() {
  const spaceId = createSpace({ slug: 'team-a', name: 'Team A', kind: 'team', ownerId })
  addSpaceMember({ spaceId, userId: ownerId, role: 'owner' })
  addSpaceMember({ spaceId, userId: memberId, role: 'member' })
  createVolume({ spaceId, kind: 'shared', userId: null, dockerName: 'dshvol-team-a-shared' })
  createVolume({ spaceId, kind: 'private', userId: memberId, dockerName: 'dshvol-team-a-member' })
  upsertInstance({ spaceId, userId: memberId, containerName: 'dsh-team-a-member' })
  return spaceId
}

test('removeSpaceMember removes only that membership', () => {
  const spaceId = seedTeamSpace()
  removeSpaceMember(spaceId, memberId)
  assert.equal(getSpaceMember(spaceId, memberId), null)
  assert.equal(listSpaceMembers(spaceId).length, 1)
})

test('deleteVolume removes the row', () => {
  const spaceId = seedTeamSpace()
  const vol = listVolumesForSpace(spaceId).find((v) => v.kind === 'private')
  deleteVolume(vol.id)
  assert.equal(listVolumesForSpace(spaceId).length, 1)
})

test('deleteInstance removes the row', () => {
  const spaceId = seedTeamSpace()
  const inst = listInstancesForSpace(spaceId)[0]
  deleteInstance(inst.id)
  assert.equal(listInstancesForSpace(spaceId).length, 0)
})

test('deleteSpace cascades all rows in one transaction', () => {
  const spaceId = seedTeamSpace()
  deleteSpace(spaceId)
  assert.equal(getSpaceBySlug('team-a'), null)
  assert.equal(listSpaceMembers(spaceId).length, 0)
  assert.equal(listVolumesForSpace(spaceId).length, 0)
  assert.equal(listInstancesForSpace(spaceId).length, 0)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/store-team-helpers.test.js`
Expected: FAIL — 至少 `removeSpaceMember is not a function`（视计划 03 覆盖面而定）

- [ ] **Step 3: 补齐缺失实现**

`src/store/spaces.js` 追加（已存在则跳过）：

```javascript
export function removeSpaceMember(spaceId, userId) {
  getDb().prepare('DELETE FROM space_members WHERE space_id = ? AND user_id = ?')
    .run(spaceId, userId)
}

/** 单事务级联删除空间的全部行；docker 资源的清理由调用方在此之前完成。 */
export const deleteSpace = getDb => null // 占位——实际实现如下，替换整行
```

正确实现（覆盖上面的占位行）：

```javascript
export function deleteSpace(id) {
  getDb().transaction(() => {
    getDb().prepare('DELETE FROM instances WHERE space_id = ?').run(id)
    getDb().prepare('DELETE FROM volumes WHERE space_id = ?').run(id)
    getDb().prepare('DELETE FROM space_members WHERE space_id = ?').run(id)
    getDb().prepare('DELETE FROM spaces WHERE id = ?').run(id)
  })()
}
```

`src/store/volumes.js` 追加：

```javascript
export function deleteVolume(id) {
  getDb().prepare('DELETE FROM volumes WHERE id = ?').run(id)
}
```

`src/store/instances.js` 追加：

```javascript
export function deleteInstance(id) {
  getDb().prepare('DELETE FROM instances WHERE id = ?').run(id)
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/store-team-helpers.test.js`
Expected: 4 PASS

- [ ] **Step 5: 提交**

```bash
git add src/store/spaces.js src/store/volumes.js src/store/instances.js test/store-team-helpers.test.js
git commit -m "feat: store cascade helpers for team space lifecycle"
```

---

### Task 2: orchestrator `removeVolume`

**Files:**
- Modify: `src/orchestrator/index.js`（追加 `removeVolume`）
- Test: `test/orchestrator-volume.test.js`

**Interfaces:**
- Consumes: 计划 01 的 `getDocker()`/`isMissingContainerError`（`src/orchestrator/docker.js`）。
- Produces: `removeVolume(dockerName)` — 删除 docker 卷；卷不存在（404）视为成功（幂等）；其余错误抛出。本计划 Task 5/6 与计划 06 依赖。

- [ ] **Step 1: 写失败的测试**

```javascript
// test/orchestrator-volume.test.js
import { test, beforeEach } from 'vitest'
import assert from 'node:assert/strict'
import { initDocker } from '../src/orchestrator/docker.js'
import { removeVolume } from '../src/orchestrator/index.js'

const calls = []
function fakeDocker({ missing = false, fail = false } = {}) {
  return {
    getVolume(name) {
      return {
        async remove() {
          calls.push(`volume.remove:${name}`)
          if (fail) throw new Error('volume in use')
          if (missing) throw Object.assign(new Error(`no such volume: ${name}`), { statusCode: 404 })
        },
      }
    },
  }
}

beforeEach(() => { calls.length = 0 })

test('removeVolume removes the docker volume', async () => {
  initDocker(fakeDocker())
  await removeVolume('dshvol-team-a-shared')
  assert.deepEqual(calls, ['volume.remove:dshvol-team-a-shared'])
})

test('removeVolume is idempotent on missing volume', async () => {
  initDocker(fakeDocker({ missing: true }))
  await removeVolume('dshvol-gone-shared') // 不抛错
})

test('removeVolume propagates other docker errors', async () => {
  initDocker(fakeDocker({ fail: true }))
  await assert.rejects(() => removeVolume('dshvol-x-shared'), /volume in use/)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/orchestrator-volume.test.js`
Expected: FAIL — `removeVolume is not a function`

- [ ] **Step 3: 实现 removeVolume**

在 `src/orchestrator/index.js` 追加：

```javascript
/** 删除 docker 卷；不存在视为成功（幂等删除），其余错误抛出由调用方重试。 */
export async function removeVolume(dockerName) {
  try {
    await getDocker().getVolume(dockerName).remove()
  } catch (err) {
    if (err?.statusCode === 404) return
    throw err
  }
}
```

（文件顶部需已导入 `getDocker`；计划 03 已导入则无需改动 import。）

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/orchestrator-volume.test.js`
Expected: 3 PASS

- [ ] **Step 5: 提交**

```bash
git add src/orchestrator/index.js test/orchestrator-volume.test.js
git commit -m "feat: idempotent docker volume removal"
```

---

### Task 3: 创建团队空间（服务 + 路由）

**Files:**
- Create: `src/spaces/team.js`
- Create: `src/spaces/team-routes.js`
- Modify: `src/spaces/routes.js`（注册 team-routes，一行）
- Test: `test/team-create.test.js`

**Interfaces:**
- Consumes: store spaces/volumes（Task 1 口径）、`requireUser`（`src/auth/middleware.js`，计划 02）、`ensureVolume`/`prepareSharedVolume`（`src/orchestrator/index.js`，计划 03）、`writeAudit`（`src/store/audit.js`）、`apiError`（`src/server.js`）。
- Produces:
  - `deriveTeamSlug(name)` → `[a-z0-9-]` slug（纯函数）
  - `createTeamSpace({ name, owner })` → space 行（含 slug 冲突重试、事务建行、docker 卷 provisioning、审计）
  - 路由 `POST /api/spaces` → 201 `{ space }`

- [ ] **Step 1: 写失败的测试**

```javascript
// test/team-create.test.js
import { test, beforeEach, vi } from 'vitest'
import assert from 'node:assert/strict'

const orchCalls = []
vi.mock('../src/orchestrator/index.js', () => ({
  ensureVolume: vi.fn(async (name) => { orchCalls.push(`ensure:${name}`) }),
  prepareSharedVolume: vi.fn(async (name) => { orchCalls.push(`setgid:${name}`) }),
}))

import { initDb } from '../src/store/db.js'
import { createUser } from '../src/store/users.js'
import { insertSession } from '../src/store/sessions.js'
import { getSpaceBySlug, listSpaceMembers } from '../src/store/spaces.js'
import { listVolumesForSpace } from '../src/store/volumes.js'
import { buildServer } from '../src/server.js'
import { loadConfig } from '../src/config.js'
import teamRoutes from '../src/spaces/team-routes.js'
import { deriveTeamSlug } from '../src/spaces/team.js'

const TOKEN = 'a'.repeat(64)
let app, userId

beforeEach(async () => {
  orchCalls.length = 0
  initDb(':memory:')
  userId = createUser({ email: 'u@x.com', handle: 'uu', displayName: 'U' })
  insertSession({ token: TOKEN, userId })
  app = await buildServer({ config: loadConfig({ NODE_ENV: 'development' }) })
  await app.register(teamRoutes, { prefix: '/api/spaces' })
})

test('deriveTeamSlug normalizes names', () => {
  assert.equal(deriveTeamSlug('My Team!'), 'my-team')
  assert.equal(deriveTeamSlug('  --Odd  Name-- '), 'odd-name')
  assert.equal(deriveTeamSlug('团队空间'), 'team') // 非 ascii 全部剥离后兜底
})

test('POST /api/spaces creates team space with volumes and owner membership', async () => {
  const res = await app.inject({
    method: 'POST', url: '/api/spaces',
    headers: { cookie: `dsh_session=${TOKEN}`, 'content-type': 'application/json' },
    payload: { name: 'Alpha Team' },
  })
  assert.equal(res.statusCode, 201)
  const { space } = res.json()
  assert.equal(space.slug, 'alpha-team')
  assert.equal(space.kind, 'team')
  const members = listSpaceMembers(space.id)
  assert.equal(members.length, 1)
  assert.equal(members[0].role, 'owner')
  const vols = listVolumesForSpace(space.id)
  assert.deepEqual(vols.map((v) => v.docker_name).sort(),
    ['dshvol-alpha-team-shared', 'dshvol-alpha-team-uu'])
  assert.deepEqual(orchCalls, ['ensure:dshvol-alpha-team-shared', 'setgid:dshvol-alpha-team-shared', 'ensure:dshvol-alpha-team-uu'])
})

test('slug collision gets numeric suffix', async () => {
  for (const payload of [{ name: 'Dup' }, { name: 'Dup' }]) {
    const res = await app.inject({
      method: 'POST', url: '/api/spaces',
      headers: { cookie: `dsh_session=${TOKEN}`, 'content-type': 'application/json' },
      payload,
    })
    assert.equal(res.statusCode, 201)
  }
  assert.ok(getSpaceBySlug('dup'))
  assert.ok(getSpaceBySlug('dup-2'))
})

test('POST /api/spaces requires auth and validates body', async () => {
  const anon = await app.inject({
    method: 'POST', url: '/api/spaces',
    headers: { 'content-type': 'application/json' }, payload: { name: 'X' },
  })
  assert.equal(anon.statusCode, 401)
  const bad = await app.inject({
    method: 'POST', url: '/api/spaces',
    headers: { cookie: `dsh_session=${TOKEN}`, 'content-type': 'application/json' },
    payload: { name: '' },
  })
  assert.equal(bad.statusCode, 400)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/team-create.test.js`
Expected: FAIL — `Cannot find module '../src/spaces/team.js'`

- [ ] **Step 3: 实现 team.js 与 team-routes.js**

```javascript
// src/spaces/team.js
import { createSpace, getSpaceBySlug } from '../store/spaces.js'
import { addSpaceMember } from '../store/spaces.js'
import { createVolume } from '../store/volumes.js'
import { ensureVolume, prepareSharedVolume } from '../orchestrator/index.js'
import { writeAudit } from '../store/audit.js'
import { apiError } from '../server.js'

export function deriveTeamSlug(name) {
  const slug = String(name ?? '').toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
  return slug || 'team'
}

export function assertTeamSpace(space) {
  if (!space) throw apiError(404, 'SPACE_NOT_FOUND', '空间不存在')
  if (space.kind !== 'team') throw apiError(400, 'PERSONAL_SPACE', '个人空间不支持该操作')
}

export function requireSpaceOwner(user, space) {
  const member = space && getMembership(space.id, user.id)
  if (!member || member.role !== 'owner') {
    throw apiError(403, 'FORBIDDEN', '仅空间所有者可执行该操作')
  }
  return member
}

// getSpaceMember 的本地别名，避免与路由层命名混淆
import { getSpaceMember as getMembership } from '../store/spaces.js'

const MAX_SLUG_ATTEMPTS = 20

/**
 * 创建团队空间：spaces + owner 成员行 + 共享卷行 + 创建者私有卷行，
 * 随后 provisioning docker 卷（共享卷置 setgid，见计划 03）。
 */
export async function createTeamSpace({ name, owner }) {
  const base = deriveTeamSlug(name)
  for (let attempt = 0; attempt < MAX_SLUG_ATTEMPTS; attempt++) {
    const slug = attempt === 0 ? base : `${base}-${attempt + 1}`
    if (getSpaceBySlug(slug)) continue
    let spaceId
    try {
      spaceId = createSpace({ slug, name: String(name).slice(0, 64), kind: 'team', ownerId: owner.id })
    } catch (err) {
      if (String(err?.message).includes('UNIQUE')) continue // 并发创建撞 slug
      throw err
    }
    const space = getSpaceBySlug(slug)
    addSpaceMember({ spaceId, userId: owner.id, role: 'owner' })
    const sharedName = `dshvol-${slug}-shared`
    const privateName = `dshvol-${slug}-${owner.handle}`
    createVolume({ spaceId, kind: 'shared', userId: null, dockerName: sharedName })
    createVolume({ spaceId, kind: 'private', userId: owner.id, dockerName: privateName })
    await ensureVolume(sharedName)
    await prepareSharedVolume(sharedName)
    await ensureVolume(privateName)
    writeAudit({ actorId: owner.id, action: 'space.create', targetType: 'space',
      targetId: spaceId, detail: { kind: 'team', slug } })
    return space
  }
  throw apiError(409, 'SLUG_EXHAUSTED', '空间名称冲突过多，请换一个名称')
}
```

```javascript
// src/spaces/team-routes.js
import { z } from 'zod'
import { requireUser } from '../auth/middleware.js'
import { createTeamSpace } from './team.js'

export default async function teamRoutes(app) {
  app.addHook('preHandler', requireUser)

  app.post('/', {
    schema: { body: z.object({ name: z.string().min(1).max(64) }) },
  }, async (req, reply) => {
    const space = await createTeamSpace({ name: req.body.name, owner: req.user })
    return reply.code(201).send({ space })
  })

  // Task 4-6 在此追加 members 与 delete 路由
}
```

`src/spaces/routes.js` 中（计划 03 创建的文件）在注册自身路由后追加：

```javascript
import teamRoutes from './team-routes.js'
// buildServer 或 spaces 插件注册处追加：
//   await app.register(teamRoutes, { prefix: '/api/spaces' })
```

注意：计划 03 的 `routes.js` 自身已是 `/api/spaces` 前缀插件；本行加在该插件内部末尾（`app.register(teamRoutes)` 不带前缀，因为外层已是 `/api/spaces`），或加在 `src/index.js`/`src/server.js` 的模块注册处（带 `{ prefix: '/api/spaces' }`）——两处任选其一，以计划 03 实际接线点为准，保持与 `spaces` 插件同前缀即可。

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/team-create.test.js`
Expected: 4 PASS

- [ ] **Step 5: 提交**

```bash
git add src/spaces/team.js src/spaces/team-routes.js src/spaces/routes.js test/team-create.test.js
git commit -m "feat: create team space with volumes and owner membership"
```

---

### Task 4: 添加成员（按邮箱，仅限已注册用户）

**Files:**
- Modify: `src/spaces/team.js`（追加 `addMemberByEmail`）
- Modify: `src/spaces/team-routes.js`（追加 `POST /:slug/members`）
- Test: `test/team-members-add.test.js`

**Interfaces:**
- Consumes: `getUserByEmail`（`src/store/users.js`，计划 02）、Task 3 的 `assertTeamSpace`/`requireSpaceOwner`、`ensureVolume`。
- Produces:
  - `addMemberByEmail({ space, email, actor })` → `{ userId, email, displayName, role: 'member' }`
  - 路由 `POST /api/spaces/:slug/members` → 201 `{ member }`；错误：404 `USER_NOT_REGISTERED` / 409 `ALREADY_MEMBER` / 400 `PERSONAL_SPACE` / 403 `FORBIDDEN`

- [ ] **Step 1: 写失败的测试**

```javascript
// test/team-members-add.test.js
import { test, beforeEach, vi } from 'vitest'
import assert from 'node:assert/strict'

vi.mock('../src/orchestrator/index.js', () => ({
  ensureVolume: vi.fn(async () => {}),
  prepareSharedVolume: vi.fn(async () => {}),
}))

import { initDb } from '../src/store/db.js'
import { createUser, getUserByEmail } from '../src/store/users.js'
import { insertSession } from '../src/store/sessions.js'
import { getSpaceBySlug, getSpaceMember } from '../src/store/spaces.js'
import { listVolumesForSpace } from '../src/store/volumes.js'
import { buildServer } from '../src/server.js'
import { loadConfig } from '../src/config.js'
import teamRoutes from '../src/spaces/team-routes.js'

const OWNER_TOKEN = 'a'.repeat(64)
const OTHER_TOKEN = 'b'.repeat(64)
let app, ownerId, otherId

beforeEach(async () => {
  initDb(':memory:')
  ownerId = createUser({ email: 'o@x.com', handle: 'oo', displayName: 'O' })
  otherId = createUser({ email: 'n@x.com', handle: 'nn', displayName: 'N' })
  insertSession({ token: OWNER_TOKEN, userId: ownerId })
  insertSession({ token: OTHER_TOKEN, userId: otherId })
  app = await buildServer({ config: loadConfig({ NODE_ENV: 'development' }) })
  await app.register(teamRoutes, { prefix: '/api/spaces' })
  await app.inject({
    method: 'POST', url: '/api/spaces',
    headers: { cookie: `dsh_session=${OWNER_TOKEN}`, 'content-type': 'application/json' },
    payload: { name: 'Team' },
  })
})

function addMember(token, email, slug = 'team') {
  return app.inject({
    method: 'POST', url: `/api/spaces/${slug}/members`,
    headers: { cookie: `dsh_session=${token}`, 'content-type': 'application/json' },
    payload: { email },
  })
}

test('owner adds a registered user by email', async () => {
  const res = await addMember(OWNER_TOKEN, 'n@x.com')
  assert.equal(res.statusCode, 201)
  assert.equal(res.json().member.role, 'member')
  const space = getSpaceBySlug('team')
  assert.ok(getSpaceMember(space.id, otherId))
  assert.ok(listVolumesForSpace(space.id)
    .some((v) => v.docker_name === 'dshvol-team-nn' && v.kind === 'private'))
})

test('inviting an unregistered email returns 404 USER_NOT_REGISTERED', async () => {
  const res = await addMember(OWNER_TOKEN, 'ghost@x.com')
  assert.equal(res.statusCode, 404)
  assert.equal(res.json().error.code, 'USER_NOT_REGISTERED')
})

test('duplicate membership returns 409 ALREADY_MEMBER', async () => {
  await addMember(OWNER_TOKEN, 'n@x.com')
  const res = await addMember(OWNER_TOKEN, 'n@x.com')
  assert.equal(res.statusCode, 409)
  assert.equal(res.json().error.code, 'ALREADY_MEMBER')
})

test('non-owner cannot add members', async () => {
  await addMember(OWNER_TOKEN, 'n@x.com')
  const res = await addMember(OTHER_TOKEN, 'o@x.com')
  assert.equal(res.statusCode, 403)
  assert.equal(res.json().error.code, 'FORBIDDEN')
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/team-members-add.test.js`
Expected: FAIL — `POST /api/spaces/team/members` 404 NOT_FOUND（路由不存在）

- [ ] **Step 3: 实现 addMemberByEmail 与路由**

`src/spaces/team.js` 追加：

```javascript
import { getUserByEmail } from '../store/users.js'

/** 按邮箱添加成员（仅限已注册用户）；同时建行并 provisioning 其私有卷。 */
export async function addMemberByEmail({ space, email, actor }) {
  assertTeamSpace(space)
  requireSpaceOwner(actor, space)
  const target = getUserByEmail(String(email).toLowerCase())
  if (!target) {
    throw apiError(404, 'USER_NOT_REGISTERED', '该用户尚未登录过平台，无法邀请')
  }
  if (getMembership(space.id, target.id)) {
    throw apiError(409, 'ALREADY_MEMBER', '该用户已是空间成员')
  }
  addSpaceMember({ spaceId: space.id, userId: target.id, role: 'member' })
  const dockerName = `dshvol-${space.slug}-${target.handle}`
  createVolume({ spaceId: space.id, kind: 'private', userId: target.id, dockerName })
  await ensureVolume(dockerName)
  writeAudit({ actorId: actor.id, action: 'space.member_add', targetType: 'space',
    targetId: space.id, detail: { email: target.email } })
  return { userId: target.id, email: target.email, displayName: target.display_name, role: 'member' }
}
```

`src/spaces/team-routes.js` 的 `teamRoutes` 内追加：

```javascript
import { getSpaceBySlug } from '../store/spaces.js'
import { addMemberByEmail } from './team.js'

app.post('/:slug/members', {
  schema: {
    params: z.object({ slug: z.string().regex(/^[a-z0-9-]+$/) }),
    body: z.object({ email: z.string().email() }),
  },
}, async (req, reply) => {
  const space = getSpaceBySlug(req.params.slug)
  const member = await addMemberByEmail({ space, email: req.body.email, actor: req.user })
  return reply.code(201).send({ member })
})
```

（`addMemberByEmail` 内部对 `space = null` 走 `assertTeamSpace` 的 404 分支。）

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/team-members-add.test.js`
Expected: 4 PASS

- [ ] **Step 5: 提交**

```bash
git add src/spaces/team.js src/spaces/team-routes.js test/team-members-add.test.js
git commit -m "feat: add team space member by email"
```

---

### Task 5: 移除成员（级联清理实例与私有卷）

**Files:**
- Modify: `src/spaces/team.js`（追加 `withRetry` 与 `removeMemberCascade`）
- Modify: `src/spaces/team-routes.js`（追加 `DELETE /:slug/members/:userId`）
- Test: `test/team-members-remove.test.js`

**Interfaces:**
- Consumes: `stopInstance(id)`、`removeContainerKeepVolumes(name)`（计划 03）、`removeVolume`（Task 2）、store Task 1 函数、`getUserById`（计划 02）。
- Produces:
  - `withRetry(fn, { attempts = 3, delayMs = 500 })` — docker 步骤重试器（`delayMs` 可注入，测试传 0）
  - `removeMemberCascade({ space, targetUserId, actor, retryDelayMs })` — 顺序：停实例 → 删容器 → 删私有卷 → 删实例行 → 删卷行 → 删成员行
  - 路由 `DELETE /api/spaces/:slug/members/:userId` → 204；错误：400 `CANNOT_REMOVE_OWNER` / 404 `MEMBER_NOT_FOUND`

- [ ] **Step 1: 写失败的测试**

```javascript
// test/team-members-remove.test.js
import { test, beforeEach, vi } from 'vitest'
import assert from 'node:assert/strict'

const seq = []
vi.mock('../src/orchestrator/index.js', () => ({
  ensureVolume: vi.fn(async () => {}),
  prepareSharedVolume: vi.fn(async () => {}),
  stopInstance: vi.fn(async (id) => { seq.push(`stop:${id}`) }),
  removeContainerKeepVolumes: vi.fn(async (name) => { seq.push(`rmc:${name}`) }),
  removeVolume: vi.fn(async (name) => { seq.push(`rmv:${name}`) }),
}))

import { initDb } from '../src/store/db.js'
import { createUser } from '../src/store/users.js'
import { insertSession } from '../src/store/sessions.js'
import { getSpaceBySlug, getSpaceMember } from '../src/store/spaces.js'
import { listVolumesForSpace } from '../src/store/volumes.js'
import { upsertInstance, getInstance } from '../src/store/instances.js'
import { buildServer } from '../src/server.js'
import { loadConfig } from '../src/config.js'
import teamRoutes from '../src/spaces/team-routes.js'

const OWNER_TOKEN = 'a'.repeat(64)
let app, ownerId, memberId, space

beforeEach(async () => {
  seq.length = 0
  initDb(':memory:')
  ownerId = createUser({ email: 'o@x.com', handle: 'oo', displayName: 'O' })
  memberId = createUser({ email: 'm@x.com', handle: 'mm', displayName: 'M' })
  insertSession({ token: OWNER_TOKEN, userId: ownerId })
  app = await buildServer({ config: loadConfig({ NODE_ENV: 'development' }) })
  await app.register(teamRoutes, { prefix: '/api/spaces' })
  await app.inject({
    method: 'POST', url: '/api/spaces',
    headers: { cookie: `dsh_session=${OWNER_TOKEN}`, 'content-type': 'application/json' },
    payload: { name: 'Team' },
  })
  await app.inject({
    method: 'POST', url: '/api/spaces/team/members',
    headers: { cookie: `dsh_session=${OWNER_TOKEN}`, 'content-type': 'application/json' },
    payload: { email: 'm@x.com' },
  })
  space = getSpaceBySlug('team')
  upsertInstance({ spaceId: space.id, userId: memberId, containerName: 'dsh-team-mm' })
})

test('removing a member cascades in order and deletes rows', async () => {
  const res = await app.inject({
    method: 'DELETE', url: `/api/spaces/team/members/${memberId}`,
    headers: { cookie: `dsh_session=${OWNER_TOKEN}` },
  })
  assert.equal(res.statusCode, 204)
  const inst = getInstance(space.id, memberId)
  assert.deepEqual(seq, [
    `stop:${inst.id}`,
    'rmc:dsh-team-mm',
    'rmv:dshvol-team-mm',
  ])
  assert.equal(getSpaceMember(space.id, memberId), null)
  assert.equal(getInstance(space.id, memberId), null)
  assert.ok(!listVolumesForSpace(space.id).some((v) => v.docker_name === 'dshvol-team-mm'))
})

test('cannot remove the owner', async () => {
  const res = await app.inject({
    method: 'DELETE', url: `/api/spaces/team/members/${ownerId}`,
    headers: { cookie: `dsh_session=${OWNER_TOKEN}` },
  })
  assert.equal(res.statusCode, 400)
  assert.equal(res.json().error.code, 'CANNOT_REMOVE_OWNER')
})

test('removing a non-member returns 404', async () => {
  const ghost = createUser({ email: 'g@x.com', handle: 'gg', displayName: 'G' })
  const res = await app.inject({
    method: 'DELETE', url: `/api/spaces/team/members/${ghost}`,
    headers: { cookie: `dsh_session=${OWNER_TOKEN}` },
  })
  assert.equal(res.statusCode, 404)
  assert.equal(res.json().error.code, 'MEMBER_NOT_FOUND')
})

test('member without instance or volume still gets removed', async () => {
  const bare = createUser({ email: 'b@x.com', handle: 'bb', displayName: 'B' })
  await app.inject({
    method: 'POST', url: '/api/spaces/team/members',
    headers: { cookie: `dsh_session=${OWNER_TOKEN}`, 'content-type': 'application/json' },
    payload: { email: 'b@x.com' },
  })
  const res = await app.inject({
    method: 'DELETE', url: `/api/spaces/team/members/${bare}`,
    headers: { cookie: `dsh_session=${OWNER_TOKEN}` },
  })
  assert.equal(res.statusCode, 204)
  assert.equal(getSpaceMember(space.id, bare), null)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/team-members-remove.test.js`
Expected: FAIL — DELETE 路由 404

- [ ] **Step 3: 实现 withRetry 与 removeMemberCascade 及路由**

`src/spaces/team.js` 追加：

```javascript
import { getUserById } from '../store/users.js'
import { getInstance, deleteInstance } from '../store/instances.js'
import { listVolumesForSpace, deleteVolume } from '../store/volumes.js'
import { stopInstance, removeContainerKeepVolumes, removeVolume } from '../orchestrator/index.js'
import { removeSpaceMember } from '../store/spaces.js'

/** docker 步骤重试：最多 attempts 次，间隔 delayMs（测试注入 0）。 */
export async function withRetry(fn, { attempts = 3, delayMs = 500 } = {}) {
  let lastErr
  for (let i = 0; i < attempts; i++) {
    try {
      return await fn()
    } catch (err) {
      lastErr = err
      if (i < attempts - 1 && delayMs > 0) {
        await new Promise((r) => setTimeout(r, delayMs))
      }
    }
  }
  throw lastErr
}

/**
 * 移除成员并级联清理：停实例 → 删容器（保留卷语义不适用——随后立即删卷）→
 * 删私有卷 → 删实例行 → 删卷行 → 删成员行。顺序固定，不可交换（spec §5）。
 */
export async function removeMemberCascade({ space, targetUserId, actor, retryDelayMs = 500 }) {
  assertTeamSpace(space)
  requireSpaceOwner(actor, space)
  const membership = getMembership(space.id, targetUserId)
  if (!membership) throw apiError(404, 'MEMBER_NOT_FOUND', '该用户不是空间成员')
  if (membership.role === 'owner') {
    throw apiError(400, 'CANNOT_REMOVE_OWNER', '不能移除空间所有者')
  }
  const target = getUserById(targetUserId)
  const inst = getInstance(space.id, targetUserId)
  if (inst) {
    await withRetry(() => stopInstance(inst.id), { delayMs: retryDelayMs })
    await withRetry(() => removeContainerKeepVolumes(inst.container_name), { delayMs: retryDelayMs })
  }
  const vol = listVolumesForSpace(space.id)
    .find((v) => v.kind === 'private' && v.user_id === targetUserId)
  if (vol) await withRetry(() => removeVolume(vol.docker_name), { delayMs: retryDelayMs })
  if (inst) deleteInstance(inst.id)
  if (vol) deleteVolume(vol.id)
  removeSpaceMember(space.id, targetUserId)
  writeAudit({ actorId: actor.id, action: 'space.member_remove', targetType: 'space',
    targetId: space.id, detail: { email: target?.email } })
}
```

`src/spaces/team-routes.js` 追加：

```javascript
import { removeMemberCascade } from './team.js'

app.delete('/:slug/members/:userId', {
  schema: {
    params: z.object({
      slug: z.string().regex(/^[a-z0-9-]+$/),
      userId: z.coerce.number().int().positive(),
    }),
  },
}, async (req, reply) => {
  const space = getSpaceBySlug(req.params.slug)
  await removeMemberCascade({ space, targetUserId: req.params.userId, actor: req.user })
  return reply.code(204).send()
})
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/team-members-remove.test.js`
Expected: 4 PASS

- [ ] **Step 5: 提交**

```bash
git add src/spaces/team.js src/spaces/team-routes.js test/team-members-remove.test.js
git commit -m "feat: remove member with instance and volume cascade"
```

---

### Task 6: 删除空间（级联 + 重试 + 失败标记）

**Files:**
- Modify: `src/spaces/team.js`（追加 `deleteSpaceCascade`）
- Modify: `src/spaces/team-routes.js`（追加 `DELETE /:slug`）
- Test: `test/team-space-delete.test.js`

**Interfaces:**
- Consumes: Task 1 的 `deleteSpace`、Task 5 的 `withRetry`、`listInstancesForSpace`/`updateInstance`、`removeContainer`（计划 03）、`removeVolume`（Task 2）。
- Produces:
  - `deleteSpaceCascade({ space, actor, retryDelayMs })` — 实例容器 → 私有卷 → 共享卷 → `deleteSpace(id)` 事务；owner 或平台 admin 可调用（路由层判定）；失败标 error + 审计 + 502
  - 路由 `DELETE /api/spaces/:slug` → 204。计划 06 管理端删除空间复用 `deleteSpaceCascade`。

- [ ] **Step 1: 写失败的测试**

```javascript
// test/team-space-delete.test.js
import { test, beforeEach, vi } from 'vitest'
import assert from 'node:assert/strict'

const seq = []
let failVolumes = 0
vi.mock('../src/orchestrator/index.js', () => ({
  ensureVolume: vi.fn(async () => {}),
  prepareSharedVolume: vi.fn(async () => {}),
  stopInstance: vi.fn(async (id) => { seq.push(`stop:${id}`) }),
  removeContainer: vi.fn(async (name) => { seq.push(`rmc:${name}`) }),
  removeContainerKeepVolumes: vi.fn(async (name) => { seq.push(`rmck:${name}`) }),
  removeVolume: vi.fn(async (name) => {
    seq.push(`rmv:${name}`)
    if (failVolumes > 0) { failVolumes--; throw new Error('volume busy') }
  }),
}))

import { initDb, getDb } from '../src/store/db.js'
import { createUser } from '../src/store/users.js'
import { insertSession } from '../src/store/sessions.js'
import { getSpaceBySlug } from '../src/store/spaces.js'
import { upsertInstance, listInstancesForSpace } from '../src/store/instances.js'
import { listAudit } from '../src/store/audit.js'
import { buildServer } from '../src/server.js'
import { loadConfig } from '../src/config.js'
import teamRoutes from '../src/spaces/team-routes.js'

const OWNER_TOKEN = 'a'.repeat(64)
const ADMIN_TOKEN = 'c'.repeat(64)
let app, ownerId, space

beforeEach(async () => {
  seq.length = 0
  failVolumes = 0
  initDb(':memory:')
  ownerId = createUser({ email: 'o@x.com', handle: 'oo', displayName: 'O' })
  const adminId = createUser({ email: 'a@x.com', handle: 'aa', displayName: 'A', role: 'admin' })
  insertSession({ token: OWNER_TOKEN, userId: ownerId })
  insertSession({ token: ADMIN_TOKEN, userId: adminId })
  app = await buildServer({ config: loadConfig({ NODE_ENV: 'development' }) })
  await app.register(teamRoutes, { prefix: '/api/spaces' })
  await app.inject({
    method: 'POST', url: '/api/spaces',
    headers: { cookie: `dsh_session=${OWNER_TOKEN}`, 'content-type': 'application/json' },
    payload: { name: 'Team' },
  })
  space = getSpaceBySlug('team')
  upsertInstance({ spaceId: space.id, userId: ownerId, containerName: 'dsh-team-oo' })
})

function del(token, slug = 'team') {
  return app.inject({ method: 'DELETE', url: `/api/spaces/${slug}`, headers: { cookie: `dsh_session=${token}` } })
}

test('owner deletes space: containers, then volumes, then rows', async () => {
  const res = await del(OWNER_TOKEN)
  assert.equal(res.statusCode, 204)
  assert.deepEqual(seq, [
    `stop:${listInstancesForSpace(space.id)[0]?.id ?? 1}`.replace('stop:1', 'stop:1'), // 见下方说明
    'rmc:dsh-team-oo',
    'rmv:dshvol-team-oo',
    'rmv:dshvol-team-shared',
  ].slice(0 - 4 === 0 ? undefined : undefined))
  assert.equal(getSpaceBySlug('team'), null)
})
```

上面的断言写法太绕——按下面的定稿测试写（替换整个 test 块）：

```javascript
test('owner deletes space: containers, then volumes, then rows', async () => {
  const instId = listInstancesForSpace(space.id)[0].id
  const res = await del(OWNER_TOKEN)
  assert.equal(res.statusCode, 204)
  assert.deepEqual(seq, [
    `stop:${instId}`,
    'rmc:dsh-team-oo',
    'rmv:dshvol-team-oo',
    'rmv:dshvol-team-shared',
  ])
  assert.equal(getSpaceBySlug('team'), null)
})

test('platform admin may delete any team space', async () => {
  const res = await del(ADMIN_TOKEN)
  assert.equal(res.statusCode, 204)
})

test('volume failure marks instance error, audits, returns 502', async () => {
  failVolumes = 99 // 所有卷删除都失败（含重试）
  const res = await del(OWNER_TOKEN)
  assert.equal(res.statusCode, 502)
  assert.equal(res.json().error.code, 'SPACE_DELETE_FAILED')
  const inst = listInstancesForSpace(space.id)[0]
  assert.equal(inst.status, 'error')
  assert.ok(inst.error.includes('volume busy'))
  assert.ok(listAudit({ action: 'space.delete_failed' }).length === 1)
  assert.ok(getSpaceBySlug('team')) // 行保留，供排查与重试
})
```

个人空间守卫测试（个人空间由计划 03 惰性创建；这里直接建行）：

```javascript
test('personal space cannot be deleted', async () => {
  getDb().prepare(
    "INSERT INTO spaces (slug, name, kind, owner_id, created_at) VALUES ('p-oo','个人','personal',?,?)",
  ).run(ownerId, Date.now())
  const res = await del(OWNER_TOKEN, 'p-oo')
  assert.equal(res.statusCode, 400)
  assert.equal(res.json().error.code, 'PERSONAL_SPACE')
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run test/team-space-delete.test.js`
Expected: FAIL — DELETE `/api/spaces/team` 404

- [ ] **Step 3: 实现 deleteSpaceCascade 与路由**

`src/spaces/team.js` 追加：

```javascript
import { listInstancesForSpace, updateInstance } from '../store/instances.js'
import { removeContainer } from '../orchestrator/index.js'
import { deleteSpace } from '../store/spaces.js'
import { isMissingContainerError } from '../orchestrator/docker.js'

/**
 * 删除空间级联（spec §5 顺序）：逐个实例停+删容器（容器已消失不视为失败）→
 * 全部私有卷 → 共享卷 → 单事务删行。任一步骤重试后仍失败：实例标 error、
 * 写 space.delete_failed 审计、抛 502。残留资源由计划 06 用量页暴露。
 */
export async function deleteSpaceCascade({ space, actor, retryDelayMs = 500 }) {
  assertTeamSpace(space)
  const isOwner = getMembership(space.id, actor.id)?.role === 'owner'
  if (!isOwner && actor.role !== 'admin') {
    throw apiError(403, 'FORBIDDEN', '仅空间所有者或平台管理员可删除空间')
  }
  const instances = listInstancesForSpace(space.id)
  try {
    for (const inst of instances) {
      await withRetry(async () => {
        try {
          await stopInstance(inst.id)
          await removeContainer(inst.container_name)
        } catch (err) {
          if (!isMissingContainerError(err)) throw err
        }
      }, { delayMs: retryDelayMs })
    }
    const vols = listVolumesForSpace(space.id)
    for (const vol of vols.filter((v) => v.kind === 'private')) {
      await withRetry(() => removeVolume(vol.docker_name), { delayMs: retryDelayMs })
    }
    const shared = vols.find((v) => v.kind === 'shared')
    if (shared) await withRetry(() => removeVolume(shared.docker_name), { delayMs: retryDelayMs })
  } catch (err) {
    const message = String(err?.message ?? err)
    for (const inst of instances) {
      updateInstance(inst.id, { status: 'error', error: `space delete failed: ${message}` })
    }
    writeAudit({ actorId: actor.id, action: 'space.delete_failed', targetType: 'space',
      targetId: space.id, detail: { error: message } })
    throw apiError(502, 'SPACE_DELETE_FAILED', `空间删除失败：${message}；请稍后重试`)
  }
  deleteSpace(space.id)
  writeAudit({ actorId: actor.id, action: 'space.delete', targetType: 'space',
    targetId: space.id, detail: { slug: space.slug } })
}
```

`src/spaces/team-routes.js` 追加：

```javascript
import { deleteSpaceCascade } from './team.js'

app.delete('/:slug', {
  schema: { params: z.object({ slug: z.string().regex(/^[a-z0-9-]+$/) }) },
}, async (req, reply) => {
  const space = getSpaceBySlug(req.params.slug)
  await deleteSpaceCascade({ space, actor: req.user })
  return reply.code(204).send()
})
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run test/team-space-delete.test.js`
Expected: 4 PASS（含定稿后的 owner-delete 用例）

- [ ] **Step 5: 提交**

```bash
git add src/spaces/team.js src/spaces/team-routes.js test/team-space-delete.test.js
git commit -m "feat: delete team space with ordered cascade, retries, and failure marking"
```

---

### Task 7: 前端——创建团队空间对话框

**Files:**
- Create: `src/web/src/components/CreateSpaceDialog.tsx`
- Modify: `src/web/src/pages/SpaceList.tsx`（顶部加按钮 + state + 跳转）

**Interfaces:**
- Consumes: `api` helper（`src/web/src/api.ts`，计划 02：`api<T>(path, { method?, body? })`，错误时抛 `Error`，消息取自错误信封）；`POST /api/spaces`（Task 3）。
- Produces: `<CreateSpaceDialog onCreated={(slug) => void} onClose={() => void} />`；SpaceList 头部出现「创建团队空间」按钮。

注意：本任务无独立后端测试可写，验证方式为 `tsc --noEmit` 通过 + 手动/构建验证（前端组件测试基础设施不在 v1 范围，spec §10 只要求服务端单测/集成测试）。步骤相应调整：先写组件，再构建验证。

- [ ] **Step 1: 写 CreateSpaceDialog 组件**

```tsx
// src/web/src/components/CreateSpaceDialog.tsx
import { useState } from 'react'
import { api } from '../api'

interface Props {
  onCreated: (slug: string) => void
  onClose: () => void
}

export function CreateSpaceDialog({ onCreated, onClose }: Props) {
  const [name, setName] = useState('')
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)

  async function submit(e: React.FormEvent) {
    e.preventDefault()
    setBusy(true)
    setError('')
    try {
      const { space } = await api<{ space: { slug: string } }>('/api/spaces', {
        method: 'POST',
        body: { name },
      })
      onCreated(space.slug)
    } catch (err) {
      setError(err instanceof Error ? err.message : '创建失败')
      setBusy(false)
    }
  }

  return (
    <form className="dialog" onSubmit={submit}>
      <h2 className="heading">创建团队空间</h2>
      <label className="field">
        <span>名称</span>
        <input
          value={name}
          onChange={(e) => setName(e.target.value)}
          maxLength={64}
          required
          autoFocus
        />
      </label>
      {error && <p className="error-text">{error}</p>}
      <div className="dialog-actions">
        <button type="button" onClick={onClose} disabled={busy}>取消</button>
        <button type="submit" disabled={busy || !name.trim()}>创建</button>
      </div>
    </form>
  )
}
```

`src/web/src/base.css` 追加（单色、无假阴影，符合 spec §8）：

```css
.dialog { border: 1px solid var(--border); border-radius: 8px; padding: 24px; max-width: 40ch; }
.heading { font-size: 16px; font-weight: 600; margin: 0 0 16px; }
.field { display: block; margin-bottom: 16px; }
.field span { display: block; font-size: 12px; color: var(--muted); margin-bottom: 4px; }
.field input {
  width: 100%; font: inherit; color: var(--fg); background: var(--bg);
  border: 1px solid var(--border); border-radius: 6px; padding: 6px 10px;
}
.error-text { color: var(--error); font-size: 13px; }
.dialog-actions { display: flex; gap: 8px; justify-content: flex-end; }
```

- [ ] **Step 2: 接线 SpaceList**

`src/web/src/pages/SpaceList.tsx`（计划 03 创建）：

```tsx
// 顶部 import 追加：
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { CreateSpaceDialog } from '../components/CreateSpaceDialog'

// 组件内追加 state 与导航：
const [creating, setCreating] = useState(false)
const navigate = useNavigate()

// 列表标题行右侧加按钮；creating 时渲染对话框（放在列表上方）：
//   <button onClick={() => setCreating(true)}>创建团队空间</button>
//   {creating && (
//     <CreateSpaceDialog
//       onCreated={(slug) => { setCreating(false); navigate(`/spaces/${slug}`) }}
//       onClose={() => setCreating(false)}
//     />
//   )}
```

- [ ] **Step 3: 构建验证**

Run: `npm --prefix src/web run build`
Expected: `tsc --noEmit` 无错误，vite build 成功

- [ ] **Step 4: 提交**

```bash
git add src/web/src/components/CreateSpaceDialog.tsx src/web/src/pages/SpaceList.tsx src/web/src/base.css
git commit -m "feat: create team space dialog in space list"
```

---

### Task 8: 前端——成员管理面板与删除空间

**Files:**
- Create: `src/web/src/components/MembersPanel.tsx`
- Create: `src/web/src/components/DeleteSpaceSection.tsx`
- Modify: `src/web/src/pages/SpaceDetail.tsx`（渲染两个组件）

**Interfaces:**
- Consumes: `GET /api/spaces/:slug`（计划 03，响应 `{ space, members }`，members 含 `userId`/`email`/`displayName`/`role`）、`GET /api/auth/me`（计划 02，`{ user: { id, ... } }`）、Task 4/6 的 members 与 delete 路由。
- Produces: `<MembersPanel slug members meId isOwner onChanged />`；`<DeleteSpaceSection slug kind onDeleted />`。

- [ ] **Step 1: 写 MembersPanel 组件**

```tsx
// src/web/src/components/MembersPanel.tsx
import { useState } from 'react'
import { api } from '../api'

interface Member {
  userId: number
  email: string
  displayName: string
  role: 'owner' | 'member'
}

interface Props {
  slug: string
  members: Member[]
  meId: number
  onChanged: () => void
}

export function MembersPanel({ slug, members, meId, onChanged }: Props) {
  const isOwner = members.some((m) => m.userId === meId && m.role === 'owner')
  const [email, setEmail] = useState('')
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)

  async function add(e: React.FormEvent) {
    e.preventDefault()
    setBusy(true)
    setError('')
    try {
      await api(`/api/spaces/${slug}/members`, { method: 'POST', body: { email } })
      setEmail('')
      onChanged()
    } catch (err) {
      setError(err instanceof Error ? err.message : '添加失败')
    } finally {
      setBusy(false)
    }
  }

  async function remove(userId: number) {
    setError('')
    try {
      await api(`/api/spaces/${slug}/members/${userId}`, { method: 'DELETE' })
      onChanged()
    } catch (err) {
      setError(err instanceof Error ? err.message : '移除失败')
    }
  }

  return (
    <section>
      <h2 className="heading">成员</h2>
      <table>
        <thead>
          <tr><th>邮箱</th><th>名称</th><th>角色</th>{isOwner && <th></th>}</tr>
        </thead>
        <tbody>
          {members.map((m) => (
            <tr key={m.userId}>
              <td>{m.email}</td>
              <td>{m.displayName}</td>
              <td>{m.role === 'owner' ? '所有者' : '成员'}</td>
              {isOwner && (
                <td>
                  {m.role !== 'owner' && (
                    <button onClick={() => remove(m.userId)}>移除</button>
                  )}
                </td>
              )}
            </tr>
          ))}
        </tbody>
      </table>
      {isOwner && (
        <form onSubmit={add} className="member-add">
          <input
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            placeholder="按邮箱添加已注册用户"
            type="email"
            required
          />
          <button type="submit" disabled={busy}>添加</button>
        </form>
      )}
      {error && <p className="error-text">{error}</p>}
    </section>
  )
}
```

- [ ] **Step 2: 写 DeleteSpaceSection 组件**

```tsx
// src/web/src/components/DeleteSpaceSection.tsx
import { useState } from 'react'
import { api } from '../api'

interface Props {
  slug: string
  kind: 'personal' | 'team'
  isOwnerOrAdmin: boolean
  onDeleted: () => void
}

export function DeleteSpaceSection({ slug, kind, isOwnerOrAdmin, onDeleted }: Props) {
  const [confirming, setConfirming] = useState(false)
  const [error, setError] = useState('')
  if (kind !== 'team' || !isOwnerOrAdmin) return null

  async function doDelete() {
    setError('')
    try {
      await api(`/api/spaces/${slug}`, { method: 'DELETE' })
      onDeleted()
    } catch (err) {
      setError(err instanceof Error ? err.message : '删除失败')
      setConfirming(false)
    }
  }

  return (
    <section>
      <h2 className="heading">危险操作</h2>
      <p className="body">删除空间将销毁全部成员实例与共享文件，不可恢复。</p>
      {!confirming ? (
        <button onClick={() => setConfirming(true)}>删除空间</button>
      ) : (
        <div className="dialog-actions" style={{ justifyContent: 'flex-start' }}>
          <button onClick={doDelete}>确认删除 {slug}</button>
          <button onClick={() => setConfirming(false)}>取消</button>
        </div>
      )}
      {error && <p className="error-text">{error}</p>}
    </section>
  )
}
```

- [ ] **Step 3: 接线 SpaceDetail**

`src/web/src/pages/SpaceDetail.tsx`（计划 03 创建）：

```tsx
// import 追加：
import { MembersPanel } from '../components/MembersPanel'
import { DeleteSpaceSection } from '../components/DeleteSpaceSection'
import { useNavigate } from 'react-router-dom'

// 组件内：me 来自计划 02 的 /api/auth/me 加载逻辑（若 SpaceDetail 尚未加载 me，
// 在本组件内补一个 useEffect fetch，与 SpaceList 的用法保持一致）。
// 在成员/实例区域渲染：
//   <MembersPanel
//     slug={space.slug}
//     members={members}
//     meId={me.id}
//     onChanged={reload}
//   />
//   <DeleteSpaceSection
//     slug={space.slug}
//     kind={space.kind}
//     isOwnerOrAdmin={me.role === 'admin'
//       || members.some((m) => m.userId === me.id && m.role === 'owner')}
//     onDeleted={() => navigate('/spaces')}
//   />
// 其中 reload 为 SpaceDetail 已有的详情重取函数；变量名以计划 03 实际代码为准。
```

- [ ] **Step 4: 构建验证**

Run: `npm --prefix src/web run build`
Expected: 无 TS 错误，build 成功

- [ ] **Step 5: 提交**

```bash
git add src/web/src/components/MembersPanel.tsx src/web/src/components/DeleteSpaceSection.tsx src/web/src/pages/SpaceDetail.tsx
git commit -m "feat: members management panel and delete space action"
```

---

### Task 9: 全量回归与收尾

**Files:**
- 无新文件。

- [ ] **Step 1: 运行全部服务端测试**

Run: `npm test`
Expected: 全部 PASS（计划 01/02/03 既有测试 + 本计划 21 个新用例无回归）

- [ ] **Step 2: 前端构建**

Run: `npm run build:web`
Expected: 成功

- [ ] **Step 3: 提交（如有遗漏文件）**

```bash
git status
# 确认工作树干净或提交剩余文件
git commit -m "chore: team spaces wrap-up" || true
```
